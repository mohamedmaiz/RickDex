import SwiftUI

enum AppColor {
    static let background = Color(.background)
    static let borderDefault = Color(.borderDefault)
    static let cardBackground = Color(.cardBackground)
    static let statusAlive = Color(.statusAlive)
    static let statusDead = Color(.statusDead)
    static let hintText = Color(.hintText)
}
